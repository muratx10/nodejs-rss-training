export const notFoundError = 'Not found';
export const routes = {
  users: '/users',
  boards: '/boards',
  tasks: '/boards/:boardId/tasks',
  login: '/login',
};

export const MAX_TEXT_LENGTH = 255;
